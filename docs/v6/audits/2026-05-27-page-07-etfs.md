# F.0 Audit · /v6/etfs vs 07-etfs.html

**Audit date:** 2026-05-27
**Live URL:** https://atlas.jslwealth.in/v6/etfs (HTTP 200)
**Mockup file:** ~/.gstack/projects/atlas-os/designs/v6-redesign-20260526-mockups/07-etfs.html
**Verdict:** major

## Section presence

| Section (from mockup) | One-line spec | Live status | Notes |
|---|---|---|---|
| Page header | "ETFs" serif 44px + breadcrumb + sub-line (N ETFs · M Atlas Leaders · ranked by composite score) | present | Live title "ETF Universe" at `text-2xl lg:text-3xl`; sub-line shows counts. Breadcrumb absent. Font smaller than 44px. |
| 6-tile hero stats strip | 6 tiles: Total / Atlas Leaders / Categories / Premium>20bps count / AUM tracked / Data-as-of | partial | Sub-line shows count inline; no 6-tile stat grid |
| Hero stories (4-col: BUY, Watch, Strong-track, New) | 4-column story block with ETF rows per action | absent | No hero stories block; `ETFsList` renders `IndustrySnapshot` directly |
| Category bands (4 cards) | 4 cards: Index / Sector / SmartBeta / Commodity — each with top-border accent color, count, avg tracking error, avg premium, top ETFs listed | absent | No category band cards on the live ETF page |
| AMC tile row (9 tiles) | AMC tiles (SBI, Nippon, UTI, ICICI, HDFC, Mirae, Axis, DSP, Kotak) showing fund count, AUM, action signal | absent | No AMC tile row; `IndustrySnapshot` is rendered but in a different layout |
| NAV-vs-price scatter | 360px scatter plot: X = NAV, Y = market price; diagonal = fair-value line; dots colored by premium/discount | absent | No NAV-vs-price scatter on the live ETF page |
| ETF card grid (3-col, 6 cards) | 6 ETF cards: ticker, full name, action badge, 100px multidim mini-chart, thesis, 4 key metrics (tracking error, premium, AUM, expense), deep-dive link | absent | No ETF card grid on the live page |
| Main ranked table | Dense table: ticker, name, category, AUM, expense ratio, tracking error, Atlas grade, 1W/6M return, composite, top-3 holdings, portfolio badge; ColumnChooser | present | `ETFsList` renders full table with `ColumnChooser` and `PortfolioBadge` |
| BubbleRiskReturnChart | Bubble chart with ETF universe | present | `BubbleRiskReturnChart` rendered in `ETFsList` |
| SignatureMatrix | Conviction matrix for ETF universe | present | `SignatureMatrix` rendered in `ETFsList` |
| IndustrySnapshot | AMC/category snapshot panel | present | `IndustrySnapshot` rendered (though differs from mockup's AMC tile layout) |
| Footnote | Data disclaimer + methodology | absent | No footnote |

## Token compliance

- [x] `ETFsList.tsx` uses semantic tokens throughout: `bg-signal-pos`, `text-signal-neg`, `border-teal`, `bg-paper-rule`, etc. Clean.
- [x] `ColumnChooser` and `PortfolioBadge` use semantic tokens. Clean.
- [x] Fonts: `font-serif`, `font-sans`, `font-mono` only. Clean.

## Component reuse

- [x] `ETFsList`, `IndustrySnapshot`, `BubbleRiskReturnChart`, `SignatureMatrix`, `PortfolioBadge`, `ColumnChooser` — all from `components/v6/`. Correct.
- [ ] Missing: `ETFCategoryBands` — no 4-card category breakdown component.
- [ ] Missing: `AMCTileRow` — no 9-tile AMC row component (distinct from `IndustrySnapshot`).
- [ ] Missing: `NAVvsPriceScatter` — no NAV-vs-market-price scatter plot component.
- [ ] Missing: `ETFCardGrid` — no 3-col ETF card grid.
- [ ] Missing: `ETFStoriesBlock` — no 4-column hero stories for ETFs.

## Data correctness

- [x] Main table: ticker, name, category, AUM, expense ratio, tracking error, grade, returns, composite — all real data from `getEtfsForDate()`.
- [x] `conviction_tape` is NEUTRAL placeholder for all ETFs (noted in code comment in `etfs.ts` — "ETF tape is v6.1 work").
- [x] `BubbleRiskReturnChart` and `SignatureMatrix` render with real ETF data.
- [ ] Category bands: absent — no category-level aggregates computed.
- [ ] AMC tiles: absent — no AMC-level aggregates (fund count per AMC, AUM per AMC) computed.
- [ ] NAV-vs-price scatter: absent — `atlas_etf_metrics_daily` has `nav_price` and `market_price` fields but they are not used in a scatter chart.
- [ ] ETF card grid: absent — no data populated.
- [ ] Hero stories: absent — no data populated.
- [ ] Premium/discount data: `tracking_error` column exists but `nav_premium_bps` (premium to NAV) is not a displayed column in the default table.

## Per-gap closure plan

1. **Category bands (4 cards) absent** — file: create `frontend/src/components/v6/ETFCategoryBands.tsx`; 4 cards (Index / Sector / SmartBeta / Commodity) each with top-border color (signal-info / signal-pos / teal / signal-warn), ETF count, avg tracking error, avg expense ratio, top 3 ETF names. Data: group the `etfs` array by `etf_category` in `ETFsList.tsx`. Wire into `ETFsList.tsx` above the bubble chart.

2. **AMC tile row absent** — file: `frontend/src/components/v6/ETFsList.tsx`; add an AMC section below the category bands: compute AMC-level aggregates from the `etfs` array (group by `amc` or `name.split(' ')[0]`). The 9-tile responsive grid: AMC name, count, AUM, action signal. Use existing data, no new query needed.

3. **NAV-vs-price scatter absent** — file: create `frontend/src/components/v6/NAVvsPriceScatter.tsx`; data: `etfs` array has `nav_price` and `market_price` if fetched from `atlas_etf_metrics_daily` (check `getEtfsForDate()` in `lib/queries/v6/etfs.ts` — add these columns if missing); render as Recharts ScatterChart with diagonal fair-value line; color dots by premium/discount. Wire into `ETFsList.tsx`.

4. **ETF card grid (3-col) absent** — file: create `frontend/src/components/v6/ETFCardGrid.tsx`; 6 cards (top by composite) each with ticker, full name, action badge, multidim mini-chart (stub or tracking-error bar), thesis (from `eli5_summary` JSONB), 4 key metrics. Wire into `frontend/src/app/v6/etfs/page.tsx` or `ETFsList.tsx` after the category bands.

5. **Hero stories (4-col) absent** — file: create `frontend/src/components/v6/ETFStoriesBlock.tsx`; 4 columns: BUY (Atlas leaders with POSITIVE state), Watch (tracking error > median), Strong track (tracking error < 10bps), New (recently added to leader list). Data from existing `etfs` array + `atlas_etf_scorecard.created_at` for "new" detection.

6. **Page title size** — file: `frontend/src/app/v6/etfs/page.tsx` line ~48; change `text-2xl lg:text-3xl` to `text-[44px]`; add breadcrumb nav (Atlas › ETFs).
