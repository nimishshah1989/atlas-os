# F.0 Audit · /v6/stocks vs 05-stocks.html

**Audit date:** 2026-05-27
**Live URL:** https://atlas.jslwealth.in/v6/stocks
**Mockup file:** ~/.gstack/projects/atlas-os/designs/v6-redesign-20260526-mockups/05-stocks.html
**Verdict:** major

## Section presence

| Section (from mockup) | One-line spec | Live status | Notes |
|---|---|---|---|
| Page header | "Stocks" serif 44px + breadcrumb + sub-line (N instruments · M investable · K Leader/Strong) | present | Live renders "Stock Universe" at `text-2xl lg:text-3xl`; sub-line shows instrument/investable/leader counts. Breadcrumb absent. Font size smaller than 44px mockup. |
| 6-tile hero stats strip | 6 tiles: Total / Investable / Leader+Strong / Firing today / Conviction depth / New cells | partial | Live renders `StocksListV6` which does NOT have a 6-tile hero stats strip at the top of the page; the page header shows counts as inline text in the sub-line, not as a grid of stat tiles |
| Hero stories (4-col: BUYs, SELLs, High-conf, Exits) | 4-column story block at top: "Entering BUY" / "Entering SELL" / "High confidence" / "Exiting" lists with 5 stocks each, context tail | absent | No hero stories section on the live page; `StocksListV6` goes straight to the filter bar and table |
| Bubble chart (RS% vs return) + 24-cell matrix (2-up) | 1.4fr bubble + 1fr cell matrix side-by-side; bubble X=RS%ile, Y=return, size=IC; matrix shows count + IC per tier×tenure cell | absent | No bubble chart or 24-cell matrix on the live stocks page |
| RS trajectories strip | List of top 10 stocks with name, 12-week RS sparkline, current RS%ile; stocks trending into Leader state | absent | No trajectories strip on the live page |
| Story cards grid (6 cards, 3-col) | 6 stock cards: ticker + full name, BUY/WATCH/AVOID action badge, multidim mini-chart, thesis text, 4 key metrics, deep-dive link | absent | No stock card grid on the live page |
| Main data table | Dense sortable table: ticker, name, sector, tier, conviction tape, returns, RS%ile, IC, composite, action; ColumnChooser | present | `StocksListV6` renders virtualized table with all these columns plus ColumnChooser; virtualized for >300 rows |
| Filter / action bar | Action filter (ALL/POSITIVE/NEUTRAL/NEGATIVE) + Tier filter (ALL/Large/Mid/Small) | present | `StocksListV6` has `ActionFilter` and `TierFilter` buttons above the table |
| Footnote | Data-as-of + methodology | absent | No footnote on the stocks page |

## Token compliance

- [x] `StocksListV6.tsx` uses semantic tokens: `bg-signal-pos`, `text-signal-neg`, `bg-paper-rule`, `text-teal`, `border-paper-rule`, etc. Clean.
- [x] `ConvictionTape` component: uses `bg-signal-pos/80`, `bg-signal-neg/80`, `bg-paper-rule` — opacity-based semantic tokens. Acceptable.
- [x] Fonts: `font-serif`, `font-sans`, `font-mono` only. Clean.
- [ ] `ChevronUp`/`ChevronDown` Lucide icons for sort indicators are fine; no token violations there.

## Component reuse

- [x] Page imports `StocksListV6`, `DataSourceBanner` from `components/v6/`. Correct.
- [x] `StocksListV6` imports `ColumnChooser`, `PortfolioBadge`, `ConvictionTape` — all v6 components.
- [ ] Missing: hero stories section — no `StockStoriesBlock` component exists.
- [ ] Missing: bubble chart — `BubbleRiskReturnChart` exists in `components/v6/` but is NOT used on the stocks list page.
- [ ] Missing: 24-cell conviction matrix — `CellMatrix` exists but not used on stocks list page.
- [ ] Missing: RS trajectories strip — no `RSTrajectoryStrip` component.
- [ ] Missing: stock card grid — no `StockCardGrid` component; individual `StockCard` markup exists in mockup but not implemented.

## Data correctness

- [x] Table: `StocksListV6` renders real data from `getStocksForDate()`; ticker, name, sector, tier, conviction tape, returns, RS%ile all populated.
- [x] Counts in page header: investable count and Leader/Strong count are computed from live data.
- [ ] 6-tile hero stat strip: absent — no firing-today count, no conviction-depth, no new-cells stats on this page.
- [ ] Hero stories (BUYs/SELLs/Exits/High-conf): absent — no data populated.
- [ ] Bubble chart: absent — no data populated.
- [ ] Story cards: absent — no data populated.

## Per-gap closure plan

1. **6-tile hero stats strip absent** — file: `frontend/src/app/v6/stocks/page.tsx`; change: add a `HeroStatsStrip` section with 6 tiles. Tiles needed: Total (already computed), Investable (already computed), Leader+Strong (already computed), Firing Today (count stocks with any POSITIVE conviction tape entry today), Conviction Depth (avg cross-rule depth), New Cells (count `atlas_signal_calls` with `direction=POSITIVE` on `snapshotDate`). Some of these require additional queries in `lib/queries/v6/stocks.ts`.

2. **Hero stories block (4-col) absent** — file: create `frontend/src/components/v6/StockStoriesBlock.tsx`; render 4 columns: "Entering BUY" (stocks where conviction changed to POSITIVE today), "Entering SELL" (NEGATIVE today), "High confidence" (cross-rule depth ≥ 4), "Exiting" (direction changed from POSITIVE/NEGATIVE to NEUTRAL). Data: `atlas_signal_calls` for today vs yesterday delta. Wire into `frontend/src/app/v6/stocks/page.tsx` below the hero stats.

3. **Bubble chart (RS% vs return) absent** — file: `frontend/src/app/v6/stocks/page.tsx` (add a section), `frontend/src/components/v6/StocksListV6.tsx` OR a wrapper; `BubbleRiskReturnChart` already exists — use it with `iid` as label, `rs_pct` as size proxy, `ret_6m` as Y, `rs_pctile_3m` as X. Wire it in 2-up layout with the 24-cell matrix below the hero stories.

4. **24-cell conviction matrix absent from stocks page** — file: `frontend/src/app/v6/stocks/page.tsx`; add `CellMatrix` component (already exists in `components/v6/CellMatrix.tsx`) in the 2-up layout with the bubble chart.

5. **RS trajectories strip absent** — file: create `frontend/src/components/v6/RSTrajectoryStrip.tsx`; render top 10 stocks by RS%ile change (largest positive delta) with a 12-week RS sparkline; sort by `rs_pctile_3m` change. Data: RS history would need `getWeeklyRSHistory()` query on `atlas_stock_metrics_daily`.

6. **Stock card grid (6 cards, 3-col) absent** — file: create `frontend/src/components/v6/StockCardGrid.tsx`; each card shows ticker, full name, action badge, multidim mini-chart, thesis text (from `generateThesis()`), 4 key metrics. Render the top 6 highest-conviction stocks. Wire into `frontend/src/app/v6/stocks/page.tsx` below the trajectories strip.

7. **Page title size** — file: `frontend/src/app/v6/stocks/page.tsx` line ~37; change `text-2xl lg:text-3xl` to `text-[44px]` to match the mockup's `.page-title` at 44px. Also add the breadcrumb nav (Atlas › Stocks).
