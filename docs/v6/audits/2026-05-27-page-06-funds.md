# F.0 Audit · /v6/funds vs 06-funds.html

**Audit date:** 2026-05-27
**Live URL:** https://atlas.jslwealth.in/v6/funds (HTTP 200)
**Mockup file:** ~/.gstack/projects/atlas-os/designs/v6-redesign-20260526-mockups/06-funds.html
**Verdict:** major

## Section presence

| Section (from mockup) | One-line spec | Live status | Notes |
|---|---|---|---|
| Page header | "Funds" serif 44px + breadcrumb + sub-line (N funds · M Atlas Leaders · K Avoid) | present | Live title "Funds Discovery" at `text-2xl lg:text-3xl`; sub-line shows counts. Breadcrumb absent. Font smaller than mockup 44px. |
| 6-tile hero stats strip | 6 tiles: Total / Atlas Leaders / Avoid / Switch proposals pending / AUM tracked / Data-as-of | partial | Page header shows 3 counts inline in the sub-line, not as a 6-tile grid. SwitchProposalsBanner shows switch count only if proposals > 0 |
| Hero stories (4-col: Switch-In, Switch-Out, Hold, New) | 4-column story block with 3-5 fund rows each, context tail text | absent | No hero stories block; `FundsList` goes straight to `IndustrySnapshot` + `BubbleRiskReturnChart` |
| AMC ladder chart + quartile breakdown | Chart card: AMC name / quartile-bar (stacked Q1-Q4) / AUM / fund count / action; plus quartile table (AMC × return window matrix) | absent | `IndustrySnapshot` component is rendered but shows a different layout (not the mockup's AMC ladder with stacked quartile bars) |
| BubbleRiskReturnChart | Bubble chart: X = max drawdown, Y = 12M return, size = AUM; labeled with AMC or fund name | present | `BubbleRiskReturnChart` rendered in `FundsList` with fund data |
| SignatureMatrix | 6-column matrix of conviction cells: each cell shows count + verdict for the fund universe | present | `SignatureMatrix` rendered in `FundsList` |
| RS trajectories strip | 5-10 funds showing NAV trajectory sparkline + 3Y CAGR change | absent | No trajectories strip on the funds list page |
| Fund cards grid (3-col) | 3-col cards: fund name, AMC, action badge (SWITCH IN / HOLD / SWITCH OUT), quartile pills (Q1-Q4 last 24mo), 2 key metrics, thesis, deep-dive link | absent | No fund card grid on the live page |
| Main ranked table | Dense table: name, category, AUM, expense ratio, 12M return (3Y proxy), peer quartile, Atlas grade, 1W return, composite, sector tilt, portfolio badge; ColumnChooser | present | `FundsList` renders full ranked table with `ColumnChooser` and `PortfolioBadge` |
| SwitchProposalsBanner | Amber banner showing pending SWITCH proposals with fund pairs and reason | present | `SwitchProposalsBanner` renders when `switchProposals.length > 0` |
| Footnote | Data disclaimer + methodology link | absent | No footnote on funds page |

## Token compliance

- [x] `FundsList.tsx` uses semantic tokens: `bg-signal-pos`, `bg-signal-neg`, `text-teal`, `border-paper-rule`, etc. Clean.
- [x] `SwitchProposalsBanner.tsx` uses `bg-signal-warn/10`, `border-signal-warn/30` etc. Clean.
- [x] Fonts: `font-serif`, `font-sans`, `font-mono` throughout. Clean.

## Component reuse

- [x] `FundsList`, `IndustrySnapshot`, `BubbleRiskReturnChart`, `SignatureMatrix`, `PortfolioBadge`, `ColumnChooser`, `SwitchProposalsBanner` — all from `components/v6/`. Correct.
- [ ] Missing: AMC ladder component — `IndustrySnapshot` exists but renders a different layout than the mockup's AMC ladder with stacked quartile bars.
- [ ] Missing: fund card grid — no `FundCardGrid` component.
- [ ] Missing: RS/NAV trajectories strip — no `FundTrajectoryStrip` component.
- [ ] Missing: hero stories block — no `FundStoriesBlock` component.

## Data correctness

- [x] Ranked table: fund name, category, AUM, expense ratio, returns, composite score, quartile proxy all populated from `getFundRowsForDate()`.
- [x] `IndustrySnapshot` snapshot data from `getIndustrySnapshot('funds')` — real data.
- [x] `BubbleRiskReturnChart`: rendered with real fund data.
- [x] `SignatureMatrix`: rendered with real signal data.
- [ ] 6-tile hero stats strip: absent — only 3 counts shown in sub-line text.
- [ ] Hero stories (Switch-In/Out/Hold/New): absent — no data populated.
- [ ] AMC ladder with quartile bars: `IndustrySnapshot` does not implement the mock's AMC quartile bar design.
- [ ] Fund card grid: absent — no data populated.
- [ ] `peer_quartile` column in table: derived from `rs_pctile_3m` as a proxy — not the actual quartile rank from `atlas_fund_scorecard`; may show inaccurate quartiles.

## Per-gap closure plan

1. **Hero stories (4-col) absent** — file: create `frontend/src/components/v6/FundStoriesBlock.tsx`; 4 columns: Switch-In candidates (funds where `atlas_switch_rules` recommends action), Switch-Out, Hold (atlas leaders), New (recently upgraded to Leader). Data from `getSwitchProposals()` (already fetched) + `getFundRowsForDate()`. Wire into `frontend/src/app/v6/funds/page.tsx` below the SwitchProposalsBanner.

2. **AMC ladder with stacked quartile bars absent** — file: `frontend/src/components/v6/IndustrySnapshot.tsx`; the mockup's "AMC ladder" section uses a stacked horizontal bar (Q1/Q2/Q3/Q4 segments) per AMC row. Current `IndustrySnapshot` renders a different layout. Change: add a mode or replace the AMC section to show: AMC name, quartile bar (stacked), AUM, fund count, action signal. Data: group funds from `getFundRowsForDate()` by AMC, compute Q1-Q4 counts from `rs_pctile_3m`.

3. **Fund card grid (3-col) absent** — file: create `frontend/src/components/v6/FundCardGrid.tsx`; render top 6 funds (2 SWITCH_IN + 2 HOLD + 2 SWITCH_OUT sorted by composite) as cards with fund name, AMC, action badge, quartile pills, 2 metrics, thesis. Wire into `frontend/src/app/v6/funds/page.tsx` below the hero stories.

4. **6-tile hero stats strip missing** — file: `frontend/src/app/v6/funds/page.tsx` line ~51; change: replace the inline sub-line stat with a proper `HeroStatsStrip` component (6 tiles). Tiles: Total, Atlas Leaders, Avoid, Switch Proposals Pending (from `switchProposals.length`), AUM tracked (sum from fund data), Data-as-of.

5. **peer_quartile accuracy** — file: `frontend/src/lib/queries/v6/funds.ts`; change: replace `rs_pctile_3m` quartile proxy with actual quartile rank from `atlas_fund_scorecard.rank_in_category` and `category_size`; compute Q1 = rank ≤ 25th %ile of category_size, etc.
