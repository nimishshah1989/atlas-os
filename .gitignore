# Secrets & certs
.env
global-bundle.pem

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/

# Tool caches (regenerated on every run)
.mypy_cache/
.pytest_cache/
.ruff_cache/

# IDE
.vscode/
.idea/

# OS
.DS_Store

# Outputs (regenerated each run)
output/
coverage.json
.coverage
htmlcov/

# Tool-internal state (not project code). The repo DOES carry its vendored skills, the
# session-start hook and project settings so laptop and cloud sessions get the same tooling.
.claude/*
!.claude/skills/
!.claude/hooks/
!.claude/settings.json
!.claude/CLAUDE.md
.claude/settings.local.json
graphify-out/
.ruflo/
.superpowers/

# Frontend
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/.env.local
frontend/.env.production
frontend/.vercel
DESIGN.md
TODOS.md
.gstack/
.worktrees/
node_modules/
package-lock.json
.claude-flow/

# Local-only working dirs (added 2026-05-27 cleanup)
.claire/
.playwright-mcp/
test-results/
.remember/

# Ad-hoc screenshot captures (use docs/v6/ for documentation images)
/*.png
/*.jpeg
/*.jpg
/*-snapshot.md
/*-fullpage.png

# Loop C rebuild resume state (local only)
scripts/foundation/.loopC_rebuild_state.json
scripts/validator_night_run.log
frontend/.next.bak*

# Local research papers + scratch reading material / HTML scratch (never commit)
/scratch/
/*.pdf
/*.txt

# serena: machine-local symbol index, regenerated by activate_project
.serena/cache/
.serena/project.local.yml

# aislop scan artifacts (tool output, not source)
.aislop/
