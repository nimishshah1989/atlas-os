SCOPE: Overnight v6 backend buildout — non-destructive writes approved

Approved by: Nimish, 2026-05-26 ~22:00 IST (verbal grant in chat)
Approved for: Single autonomous overnight session (until 2026-05-27 ~10:00 IST)
Approved operations:
  - Alembic migrations applied via `alembic upgrade head` against live Supabase atlas-os
    (project_id: nanvgbhootvvthjujkvs)
  - ALTER TABLE (add column, add constraint, add index) — non-destructive
  - CREATE TABLE, CREATE MATERIALIZED VIEW, CREATE INDEX
  - INSERT, UPDATE for backfills (atlas_mf_recommendation_daily, atlas_etf_scorecard
    expand 34→126, atlas_sector_metrics_daily 5y backfill of new columns,
    atlas_cell_definitions.display_name + explain_text backfill)
  - REFRESH MATERIALIZED VIEW (for new MVs)

EXPLICITLY EXCLUDED (will NOT be done without separate explicit approval):
  - DROP TABLE (the 8 ATLAS-REVIEW-UNUSED candidates wait for morning sign-off)
  - DELETE FROM, TRUNCATE
  - DROP COLUMN
  - ALTER COLUMN that changes type or nullability of a populated column
  - Supabase MCP `apply_migration` tool (use Alembic via Bash)
  - Any operation against `atlas_paper_portfolio` or `atlas_user_lots` (RLS-protected user data)

CHECKPOINT DISCIPLINE: every PR / migration commits to git immediately so nothing is
lost if the session crashes.
