"""Atlas agent contexts.

Bounded context for autonomous agents that run read-only checks, validations,
and quality-assurance passes against the Atlas database and frontend.

Public surface: import from ``atlas.agents.validator`` — do not import
sub-module internals directly.
"""
