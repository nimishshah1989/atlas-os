"""SP03: OpenBB BYO Copilot integration package.

Public surface: ``atlas.api.openbb.router.openbb_router`` — mounted in
``atlas/api/__init__.py``. No imports here so the package marker stays empty
and pyright doesn't trip on circular handler imports.
"""
