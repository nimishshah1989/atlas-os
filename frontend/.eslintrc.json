{
  "extends": "next/core-web-vitals",
  "overrides": [
    {
      "files": [
        "src/components/v6/**/*.{ts,tsx}",
        "src/lib/queries/v6/**/*.{ts,tsx}"
      ],
      "rules": {
        "no-restricted-syntax": [
          "error",
          {
            "selector": "CallExpression[callee.name='Number']",
            "message": "Do not use Number() to convert Postgres NUMERIC strings in v6 paths — use toNumber(x) or toNumberOr(x, fallback) from @/lib/v6/decimal instead. Number() silently returns NaN on invalid input; toNumber() throws TypeError so chart failures are visible."
          }
        ]
      }
    }
  ]
}
