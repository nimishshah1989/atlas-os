'use client'

export function SectorBubbleChart({ data, range }: { data: unknown[]; range: string }) {
  return (
    <div className="h-[500px] border border-paper-rule rounded-sm flex items-center justify-center text-ink-tertiary text-sm">
      Bubble chart coming
    </div>
  )
}
