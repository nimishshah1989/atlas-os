'use client'

export function SectorHeatmap({ history, sectors }: { history: unknown[]; sectors: string[] }) {
  return (
    <div className="border border-paper-rule rounded-sm p-4 text-ink-tertiary text-sm">
      Heatmap coming
    </div>
  )
}
